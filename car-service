server:
  port: 6607
 
spring:
  datasource:
      url: jdbc:mysql://localhost:3306/passengerservice
      username: root
      password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6601/eureka
